import { TestBed } from '@angular/core/testing';
import { TablapartidokService } from './tablapartidok.service';

describe('TablapartidokService', () => {
  let service: TablapartidokService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(TablapartidokService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});

describe('TablapartidokService', () => {
  let service: TablapartidokService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(TablapartidokService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});




